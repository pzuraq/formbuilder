<%= form_for ([@form.group,@form]) do |f| %>
  <% if @form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <ul>
      <% @form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field_tag(:group_id, @form.group_id) %>
  <div class="field">
    <%= f.label "Form Name:" %><br>
    <%= f.text_field :name %>
  </div>
  <br />
  <p><strong>The template and everything should go here</strong></p>
  <br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= link_to 'Back', group_path(:id => @form.group_id) %>
